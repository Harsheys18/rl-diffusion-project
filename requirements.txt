# Core ML stack (DO NOT pin torch / CUDA on Kaggle)
diffusers>=0.27.0
transformers>=4.40.0
accelerate>=0.28.0
peft>=0.10.0

# Vision + models
timm>=0.9.0
open_clip_torch>=2.24.0
lpips>=0.1.4

# HuggingFace ecosystem
huggingface_hub>=0.23.0
safetensors>=0.4.0

# Utilities
tqdm>=4.66.0
ftfy>=6.1.1
regex>=2023.0.0
requests>=2.31.0

# Visualization / logging (optional but useful)
matplotlib>=3.7.0
Pillow>=9.5.0

# Optional (recommended for memory optimization)
xformers>=0.0.25